# Classes
* Classes have instance fields describing the state of the object
	* Objects are created by invoking the constructor which assigns values to instance fields
## Instance vs Static
* **Instance fields** describe 